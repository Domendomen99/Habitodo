package com.unimore.habitodo.controllo;

import android.view.View;
import android.widget.Toast;

public class ControlloDue {

    /*
    private View.OnClickListener azioneMostraMessaggio = new AzioneMostraMessaggio();

    public View.OnClickListener getAzioneMostraMessaggio() {
        return azioneMostraMessaggio;
    }

    private class AzioneMostraMessaggio implements View.OnClickListener {
        @Override
        public void onClick(View view) {
            Toast.makeText(view.getContext(), "Ciao", Toast.LENGTH_SHORT).show();
        }
    }
     */
}
